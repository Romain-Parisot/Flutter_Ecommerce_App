import 'package:flutter/widgets.dart';

@Deprecated(
  'Burger menu replaced by PrimaryNavBar. This file remains for legacy references.',
)
class BurgerNavMenu extends StatelessWidget {
  const BurgerNavMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
